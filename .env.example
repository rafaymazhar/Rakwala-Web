# Copy to `.env` in this folder (repo root) for Vite. Restart `npm run dev` after changes.

# Required for dashboard map + street view (enable "Maps JavaScript API" in Google Cloud)
VITE_GOOGLE_MAPS_API_KEY=

# Optional: only for intentional offline guest login
# VITE_ALLOW_DEMO_LOGIN=true
